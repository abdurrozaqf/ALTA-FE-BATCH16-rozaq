import {
  getBooks,
  getDetailBook,
  createBook,
  updateBook,
  deleteBook,
} from "./api";
import { Book } from "./types";

export { getBooks, getDetailBook, createBook, updateBook, deleteBook };
export type { Book };
